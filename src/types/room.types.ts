export type RoomDimensions = {
  width: number; // mm
  height: number; // mm
  length: number; // mm
};

export const DEFAULT_ROOM_DIMENSIONS: RoomDimensions = {
  width: 3000,
  height: 2500,
  length: 4000,
};

